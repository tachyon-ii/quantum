# docs/ — the un-promoted shelf, in sections

Nothing here has crossed the wall. Each section is one file or one folder; a
thing lives in one place and everything else points at it.

| section | file | what it holds |
|---|---|---|
| **ontology** | `Φ-a-minimal-ontology-of-an-emergent-universe.md` | the chain Ω → Φ{μ,i} → {m,l,s} → {E,F} → V⊥M → Universe. **The only copy.** Every other document starts by pointing here. |
| **binding** | `three-term-binding.md` | nuclei as packed capsids: §4 nomenclature onward. §0 is a pointer to Φ, not a restatement (cut 2026-09-10). |
| **edge** | `now.md` | where the argument currently is: what is graded, what is owed, what the last bee said. Read this first if you have five minutes. |
| **background/** | `background/` | James's priors and prior work — hubrility, the cyclotron, forgetting, the collapse note, Duffield's detective notes. Read for the *why*; cite nothing from here as a result. |
| **_condemned/** | `_condemned/` | superseded or throwaway — kept for the record, never for reading. A file arrives here only after its live points are harvested into `../todo/`. |
| `agents` | symlink → `TRIGZI/docs/agents` | the constitution; not ours to edit here. |

Questions with tests attached are not documents: `../specs/`. Things owed, not
yet specced: `../todo/`.

**Rule.** If you find yourself pasting the Ω → Φ paragraph into a new file, stop
and write `see Φ §1–§3` instead. Two copies drift; one is canon.
