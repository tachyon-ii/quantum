# Existing-Data Confrontations — the annals as test set

**Grace Bayes, 2026-08-25.** James's principle, adopted as protocol: *any data not used in the generative phase is test data — existing test data.* One guard governs everything below: **existing data is test data only under a frozen analysis protocol.** The moment an offset receives a post-hoc correction, test data becomes training data with extra steps. Offsets stand as strikes unless a frozen re-analysis resolves them with zero new knobs (the EHT rule, §7).

## 1. Σm_ν = 58.59 meV — CONFRONTATION LIVE NOW [the sharpest item on the board]

Current state of the field (checked 2026-08-25): DESI DR2 + Planck (ΛCDM) gives Σm_ν < 64 meV (95%); tightest combinations push < 50 meV; the tension between cosmological bounds and the terrestrial NH floor (~59 meV) is quantified at **2.5–5σ** depending on dataset/metric. Meanwhile a joint DESI-DR2+DES analysis reports a **positive-mass preference at 2.7σ**, and cosmology now **strongly prefers normal ordering (Bayes factor ≈ 46.5 vs inverted)**.

Scorecard for our number:
- **Our prediction IS the NH floor** (58.59 ≈ 59 meV, m₁ = 0.37 meV). We predict the *minimum* value nature can take in NH.
- **Ordering: VALIDATED direction.** The signed-branch solution was unique in NH only (cycle 3). Cosmology's Bayes-46.5 preference for NH is a passed existing-data test.
- **Magnitude: in the kill zone.** ΛCDM bounds are pressing at/below our number. If ΛCDM holds and bounds settle < 55 meV, the prediction dies — *and so does terrestrial NH itself*, which is the Box-Model caveat: the bound is ΛCDM-conditional (w₀wₐ extensions relax it ~3×). Our falsifier is entangled with the standard cosmology's own crisis. Watch, don't wait: this resolves on DESI's timescale, not CMB-S4's.

## 2. Koide's inherited prospective pass [credit where due]

Q = 2/3 with (m_e, m_μ) predicts **m_τ = 1776.97 MeV** — published 1981, a decade before precision tau mass. Measured: 1776.86 ± 0.12 → **+0.9σ**. A genuine pre-registered hit inherited by any theory that derives Q = 2/3. Logged as the program's strongest existing-data pass.

## 3. Sargent lifetime ladder [passed, existing data]

τ_μ/τ_τ = (m_τ/m_μ)⁵ × channel count to ~1% (cycle 2). Existing data, zero parameters, passed.

## 4. HERA archives vs the null + 4-fold signature [ready to run, protocol frozen]

The model now *predicts* no coherent F₂ oscillation at Δx ≳ 0.01 (Q_geom ∈ [7,74] ⟹ Δx ∈ [0.0004, 0.004]) and 4-fold-only harmonics (never 3k/N, 5k/N). Frozen test against existing HERA residuals (`../models/hera_explorer/`, `deprecated/hera/`): coarse coherent structure with stable phase = falsification. The archive can kill this today; it cannot confirm it (armored channel).

## 5. Nuclear existing-data targets [from the capsid audit, standing]

- **⁸Be:** pre-registered two-sided vice — the pore-channel term must land α–α at −0.092 MeV within the frozen +1.67 ± 0.1 MeV window (existing AME data; a cancellation to 4%).
- **Charge radii:** r_ch(³He) = 1.97 fm, r_ch(⁴He) = 1.68 fm as *joint* targets with binding (existing measured values; the model's Coulomb split error already points here).
- Passed already: A=5 instability (structural), He4 closure, capacity law on the A≤4 ladder.

## 6. π/12 status under this protocol

δ_l (charged masses) and δ_ν (Δm² + signed branch) both used generative data; the *relation* Δδ = π/12 (0.25%, inside 1σ) was discovered, not fitted — zero adjustable parameters. It is a sharp postdiction, not a test pass; its test is the Bridge derivation, not more data.

## 7. The EHT rule [James's specimen, adopted as protocol]

James's EHT analysis: expected peak/trough structure present, **positions offset** — neither the desired result nor the team's claimed one. This is the canonical *dangerous middle*: close enough to tempt a rationalizing "phase correction," off enough to be a falsification. Rule: **an offset anomaly stands as a strike unless a frozen re-analysis (protocol written before re-looking) resolves it with zero new degrees of freedom.** No post-hoc phase shifts, ever. The QPO/EHT materials (`tests/hfqpo_phi_pmode/`, `black-holes-v4.md`) inherit this rule if reopened.
